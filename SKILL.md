---
name: pdf-to-database
description: 表格型 PDF（政府价格文件/公报/统计年鉴/连续出版物/批量报表）转结构化数据库（JSON/SQLite/CSV）的方法论与工具箱。当用户要求从 PDF 提取表格数据、批量 PDF 数字化入库、PDF 数据建库建查询系统，或已抽取数据出现错位/丢列/伪列污染时使用。含六阶段工作流、防塌缩四防御、拼行双价拆分正则、原件逐字比对与交付体检清单。
agent_created: true
---

# PDF 转数据库：表格型 PDF 结构化抽取方法论与工具箱

## 定位与边界

适用于：**版式化表格 PDF → 结构化数据**，尤其是连续出版物（每期版式稳定但会漂移）和批量同源报表。典型来源：政府价格文件、公报、统计年鉴、行业月报。

不适用：任意版式一键转换（不存在，巨头 OCR 产品也一样）；纯文字 PDF 直接用文本层读取即可，无需本流程。

核心认知：**每个 PDF 源都有自己的版式，通用解析器不存在**。本 skill 的价值是把"探针 → 逐版式击破 → 体检交付"标准化，并把最容易翻车的四类陷阱内建到骨架里。新源适配成本约 1~3 小时。

## 六阶段工作流

### 阶段 1：侦察
- `python scripts/probe_tables.py <pdf> --sample 10` 看三件事：
  1. **无文本层页**（chars<20）→ 转曲/扫描件，需要 OCR 兜底（见阶段 2b）
  2. **列数分组** → 同列数的页大概率同版式；不同列数页 = 不同版式
  3. 表头首行关键词 → 确认表格类型（材料/劳务/机械…）
- 文件名陷阱：政府站下载的原始文件名（如 P02026...pdf）会被 glob 标准命名漏掉，期号等元数据改从正文"总第N期"回退解析。

### 阶段 2：逐版式写解析器
- 每种版式抽 2~3 页与 PDF 原件人工比对后再写代码；先单页跑通再全量。
- 直接改 `scripts/extractor_skeleton.py`（标了「按版式定制」的位置），四类防御已内建勿删：
  - **A 合并单元格继承**：县名/分组名只在首列出现，需向后继承
  - **B 碎片行跳过**：表头收集时跳过 ≤2 字、无数字、非关键字的行（竖排"单位"拆出的"单"字会中断收集 → 县名列全空 → 连锁塌缩）
  - **C 匿名价格列防线**：area 为 None 的价格列**禁止** `area or default_area` 兜底——这正是"XX地区"伪列吞掉全部县价的根源；正确做法是无县名且匿名价格列≥2/3 时拒绝整页或丢弃价格列
  - **D 拼行双价拆分**：`'350.00350(茶卡398)'` 必须拆对，正则见骨架 `split_glued_price`（group1 限定「整数+恰好两位小数」完整形态防贪婪错拆；右列该行已有数字则不拆）
- 骨架按「单层表头：列头即县名」实现；源 PDF 若是**多层表头**（县名行+含税/除税行、标题行+表头+地区名行），把表头部分改为"表头块收集"，完整实现与坑位对照见 `references/case-qinghai.md` 的 Bug⑦/⑨。

### 阶段 2b：转曲/扫描件 OCR 兜底
- 文本层为空的页走 OCR（RapidOCR/PaddleOCR），**必须做页级 JSON 缓存**（可删可重建，重跑免重 OCR）。
- OCR 坐标抽 compared to 文本层多了拆字/误识两类问题：碎片行（防御 B）、县名混淆字（如"天峻"→"天骏"，建 CountyFix 映射，先与原件核对确认是误识还是印刷错误）。
- OCR 器的县名/列名归一化逻辑必须与文本层抽取器**保持一致**（同一套 norm 函数两处引用）。

### 阶段 3：后处理合并
- 双表结构（同一数据拆成"含税价格表"+"除税价格表"两张独立表）：按"表序重启点"找边界（表2首行≈表1前3行内的同名行），difflib 序列对齐，匹配率 <0.75 保守跳过。
- A/B 双页表（首页有名称列、续页仅价格列）：行数严格相等时按行索引配对；行数不等时先查"分组行只在首页出现"再配。
- 列名归一化：剥括注（海晏县(含西海镇)→海晏县）+ 别名映射（前后页写法不一致会裂成两列）。

### 阶段 4：全量抽取
- 批量脚本记录每期条目数与提示数，失败清单断点续跑；OCR 期名单要同时覆盖带/不带 .pdf 后缀的目录名。

### 阶段 5：体检交付（必做，缺这步的数据不可信）
- `python scripts/audit_template.py <数据目录> --baseline <旧版目录>`：
  1. **伪列共现**：地区列与县列共存 = 塌缩。⚠️ 单地区统一价表 area='XX地区' 是合法形态，判据是"共存"而非"出现地区字样"，一刀切会误报数百处
  2. 表头/行值 area 一致性
  3. 缺失率（与既有口径对比判断）
  4. **基线对比**：按 section 比行数/价格点数，正向=修复恢复、负向=排查丢数据；incl/excl 计数与基线一致的 flag 是既有形态非回归
- **原件逐字比对**：对可疑值（离群/反常规律）用 words 按 y 坐标聚集原文核对。反常数值是原件本身时**保留**，不要顺手修正。
- 最后重建聚合索引（index/趋势），核对期数无空期无重复。

### 阶段 6：增量更新
- 新期到达 → 同版式直接跑 → 新版式（列数变化）→ 回阶段 1 探针 → 小步适配。
- 保留探针与体检脚本在 tools/ 目录，它们是长期资产。

## 经验规则速查（症状 → 检查点）

| 症状 | 优先检查 |
|------|---------|
| 价格列整列丢失/只剩一列 | 防御 C 塌缩：搜代码里的 `area or default` |
| 县名行为空 | 防御 B：表头收集是否被碎片行中断 |
| 数值像 '350.0035'/'0' 错位 | 防御 D：双价拆分正则 group1 是否贪婪 |
| 部分页数据缺失 | A/B 双页配对条件；表题只在首页；非标准文件名 glob |
| OCR 期与文本期同一列名不同 | 县名归一化逻辑是否两处一致；印刷错误 vs OCR 误识 |
| 体检误报大面积"伪列" | 单地区统一价表是合法形态，改判据为"共存" |

## 参考资源

- `references/case-qinghai.md` —— 完整实战复盘：青海造价信息 40 期项目，10 个 Bug 的症状→机制→修法对照表、拼行双价正则细节、原件层面的坑、交付体检清单。写解析器前通读一遍可避开绝大多数坑。
- `scripts/probe_tables.py` —— 阶段 1 侦察探针
- `scripts/extractor_skeleton.py` —— 阶段 2 抽取器骨架（四防御内建）
- `scripts/audit_template.py` —— 阶段 5 体检模板（含基线回归对比）
